import chai, { expect } from 'chai'
import recipes from './recipes'

describe('seed button', () => {
  const reducer = recipes
  const initialState = []

  it('returns an array with recipes', () => {
    // expect(reducer) (:link_or_button, 'Seed Recipes!')
  })
})
